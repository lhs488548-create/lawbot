# lawbot runtime dependencies (Python 3.12, WSL venv at .venv).
# Owner: Contracts. Pinned to versions verified in the project venv.
# Install (do NOT recreate the venv):
#   cd /home/user1/lawbot && ~/.local/bin/uv pip install --python .venv/bin/python -r requirements.txt

# OpenAI embeddings + chat (Batch API, Structured Outputs).
openai==2.41.1

# Vector store client (dense search + payload filters).
qdrant-client==1.18.0

# API service.
fastapi==0.137.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32   # multipart form parsing for /v1/ad-review uploads

# Models / validation.
pydantic==2.13.4

# Config / secrets.
python-dotenv==1.2.2

# Parsing.
PyYAML==6.0.3              # YAML front-matter
tiktoken==0.13.0           # token-accurate chunk sizing
pypdf==6.13.2              # PDF text extraction for /v1/ad-review

# Resilience / rate limiting.
tenacity==9.1.4            # retry OpenAI/Qdrant calls
slowapi==0.1.10            # per-key / per-IP rate limiting

# HTTP (test client, optional outbound calls).
httpx==0.28.1
